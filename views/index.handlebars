<!-- search bar -->
<div class='container search-bar'>
  <div class='search-bar-outer'>
    <div class='search-bar-inner'>
      <form action='/search/'>
        <h4 class='mb-2'><label for='searchRestaurant'>尋找餐廳：</label></h4>
        <div class='input-group'>
          <input id='searchRestaurant' type='text' name='keyword' id ='keyword' class='form-control' autocomplete='off'
            placeholder='輸入餐廳、分類' aria-label='Restaurant Name...' value='{{ keyword }}'
            aria-describedby='search-button' />
          <div class='input-group-append'>
            <button class='btn btn-major' type='submit' id='search-button'><i class='fas fa-search'></i></button>
          </div>
        </div>
      </form>
      {{#if error }}
      <div class="alert alert-warning" role="alert">
         您沒有輸入關鍵字唷！
      </div>
      {{/if}}
    </div>
  </div>
</div>

               
{{#if alert}}
<div class="mt-4 text-center">
  <p>-----沒有符合的搜尋，或許以下有你感興趣的餐廳？-------</p>
</div>
{{/if}}
<!-- restaurant list -->
<div class='container mt-5'>
  <div class='row'>
    <div class='col-md-10 col-12 mx-auto'>
      <div class='card-columns m-1'>
        {{#each restaurants}}
        <a href='/restaurant/{{this._id}}' class='text-secondary'>
          <div class='card shadow mb-3'>
            <img class='card-img-top' src='{{this.image}}' alt='{{this.name}}' />
            <div class='card-body m-2 p-3'>
              <h6 class='card-title mb-1'>{{{this.name}}}</h6>
              <div class='restaurant-category mb-1'>
                <i class='fas fa-utensils pr-2'></i>
                {{this.category}}
              </div>
              <span class='badge badge-pill badge-danger font-weight-normal'>
                {{this.rating}}
                <i class='fas fa-star fa-xs'></i>
              </span>
            </div>

            <div class="card-footer d-flex justify-content-end align-items-baseline">
              <a href="./restaurant/{{ this._id }}/edit" class="">編輯</a>
              <a href="./restaurant/{{this._id }}" class="mx-3">看更多</a>
              <form action="/restaurant/{{this._id }}?_method=DELETE" method="post">
                <button type="submit" class="btn btn-primary" onclick="return isDelete()">刪除</button>
              </form>
            </div>

          </div>


        </a>

        {{/each}}
      </div>
    </div>
  </div>
</div>

<script>
  function isDelete() {
    return confirm('確定要把這間餐廳刪除嗎?')
  }
</script>